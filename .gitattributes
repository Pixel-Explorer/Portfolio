# NO LFS. Vercel deploys LFS pointers as raw text (no smudge), so any
# LFS-tracked asset 404s-in-spirit on prod — the city GLB served as a 133-byte
# pointer and the site silently fell back to the procedural prisms.
# Every deployable binary stays plain git and under GitHub's 100 MB file limit
# (city.glb is ~44 MB). Oversized sources live outside the repo (bin/, Blob).
